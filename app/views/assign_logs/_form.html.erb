<%= form_for(@assign_log, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @assign_log.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@assignlog.errors.count, "error") %> prohibited this developer from being saved:</h4>

      <ul>
        <% @assign_log.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label "開発者名リスト:", class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
        <%= f.collection_select :developer_id, Developer.all, :id, :name %>
    </div>
  </div>
    <div class="form-group">
    <%= f.label "プロジェクト名リスト:", class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
        <%= f.collection_select :project_id, Project.all, :id, :name %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit "作成", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
